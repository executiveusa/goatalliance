{
  "version": 2,
  "framework": "nextjs",
  "regions": ["iad1"],
  "installCommand": "yarn install --frozen-lockfile",
  "buildCommand": "yarn build",
  "devCommand": "yarn dev",
  "env": {
    "VERCEL_TOKEN": "@vercel-token"
  }
}
